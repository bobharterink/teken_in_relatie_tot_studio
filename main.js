window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer');
    var popupImage = document.getElementById('popupImage');
    var clickArea = document.getElementById('hoofdstuk1');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });
  

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer1');
    var popupImage = document.getElementById('popupImage1');
    var clickArea = document.getElementById('hoofdstuk2');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
      if (popupContainer.style.display === 'none') {
        popupContainer.style.display = 'block';
      } else {
        popupContainer.style.display = 'none';
      }
    });
  });
  

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer2');
    var popupImage = document.getElementById('popupImage2');
    var clickArea = document.getElementById('hoofdstuk3');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });
  
  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer5');
    var popupImage = document.getElementById('popupImage5');
    var clickArea = document.getElementById('hoofdstuk6');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer6');
    var popupImage = document.getElementById('popupImage6');
    var clickArea = document.getElementById('hoofdstuk7');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer7');
    var popupImage = document.getElementById('popupImage7');
    var clickArea = document.getElementById('hoofdstuk8');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer8');
    var popupImage = document.getElementById('popupImage8');
    var clickArea = document.getElementById('hoofdstuk9');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer9');
    var popupImage = document.getElementById('popupImage9');
    var clickArea = document.getElementById('hoofdstuk10');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer10');
    var popupImage = document.getElementById('popupImage10');
    var clickArea = document.getElementById('hoofdstuk11');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });

  window.addEventListener('load', function() {
    var popupContainer = document.getElementById('popupContainer11');
    var popupImage = document.getElementById('popupImage11');
    var clickArea = document.getElementById('hoofdstuk12');
  
    // Afbeelding automatisch laten verschijnen na 3 seconden
    setTimeout(function() {
      popupContainer.style.display = 'none';
    });
  
    // Afbeelding laten verdwijnen/verschijnen wanneer erop wordt geklikt
    clickArea.addEventListener('click', function() {
        if (popupContainer.style.display === 'none') {
          popupContainer.style.display = 'block';
        } else {
          popupContainer.style.display = 'none';
        }
    });
  });